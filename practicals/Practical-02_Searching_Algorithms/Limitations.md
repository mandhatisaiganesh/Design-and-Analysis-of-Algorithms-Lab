# Limitations - Practical 02

## Linear Search
- Slow for large datasets (\(\mathcal{O}(n)\)).

## Binary Search
- Requires pre-sorted array.
- Requires random access to elements in \(\mathcal{O}(1)\) time (inefficient for linked lists).
